# D2C Intel - GitHub Actions Pipeline Dependencies
# =================================================

# --- Core: Brave Search data collection ---
requests>=2.31.0

# --- Core: Claude API for report generation & translation ---
anthropic>=0.42.0

# --- Config: YAML query definitions ---
pyyaml>=6.0.1

# --- HTML parsing / Scrapling-compatible enrichment ---
lxml>=5.3.0
cssselect>=1.2.0
tld>=0.13.1
w3lib>=2.2.1

# --- Email: Report delivery ---
sendgrid>=6.11.0

# --- Deep Fetch: Scrapling for 2nd-pass URL enrichment (optional) ---
# Install with: pip install "scrapling[fetchers]"
# scrapling>=0.4.2

# --- Playwright: PDF generation (installed via npx, not pip) ---
# npx playwright install chromium --with-deps
